spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: leave
    password: leave
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console